<table class="table table-striped">
    <tr>
        <th class="col-xs-1"></th>
        <th>{_dictionary.main.Title}</th>
        <th></th>
    </tr>
    {foreach $pages as $page}
        <tr>
            <td class="col-xs-1">
                {if $page->pages_templates_id != 9}
                    <a href="{link delete!, id => $page->id, type => $type}" onclick="return confirm('Opravdu smazat?')">
                        <i class="fa fa-times font-red"></i>
                    </a>
                {/if}

                <a n:href="public! 'id' => $page->id, type => $type" class="btn btn-default btn-xs">
                    {if $page->public == 0}
                        <span title="Povolit">
                            <i class="fa fa-toggle-off"></i>
                        </span>
                    {else}
                        <span title="Zakázat">
                        <i class="fa fa-toggle-on"></i>
                    </span>
                    {/if}
                </a>
            </td>
            <td>
                <a href="{plink ':Admin:Pages:detail', id => $page->id}">{$page->title}</a>
                {if $page->slug == ''} (homepage){/if}
            </td>
            <td>                &nbsp;
                <a href="/{$page->slug}" class="btn btn-default btn-xs" target="_blank"
                   title="{_dictionary.main.Slug}: {$settings['site:url:base']}/{$page->slug}">
                    {_dictionary.main.Preview}
                </a>
            </td>
        </tr>
    {/foreach}
</table>