{block #titles}{_dictionary.main.Contacts}{/block}
{block content}

{control pageTopMenu}

<h1 class="page-header">{_dictionary.main.Contacts} - {$page->title}</h1>

{foreach $flashes as $flash}
    <br />
    <div class="flash {$flash->type}">{$flash->message}</div>
    <br />
{/foreach}

<h2>{_dictionary.main.OpeningHours}</h2>

<p class="text-info">Pokud přidáte nový záznam k již zadanému dni, starší záznam bude přepsán.</p>
<br />
{control insertHour}

<table class="table">
    {foreach $hours as $open}
        <tr>
            <td class="col-xs-1"><a n:href="deleteHour! id => $open->id, page => $presenter->getParameter('page')"><i class="fa fa-times font-red"></i></a></td>
            {var $oh = Filters::numericday($open->day)}
            <td class="col-md-1">{_ $oh}</td>
            <td>{$open->hourstext}</td>
        </tr>
    {/foreach}
</table>